<nav class="navbar navbar-expand-lg navbar-dark bg-color">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#ntoggle" aria-controls="ntoggle" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
        <div class="collapse navbar-collapse" id="ntoggle">
            <a class="navbar-brand" href="/">
            <img src="<%= bot.avatar %>" alt="<%= bot.name %>" width="30" height="30" class="d-inline-block align-top">
            <b><%= bot.name %></b>
            </a>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="/commands">
                    Commands</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="<%= bot.invite %>">
                    Invite</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="<%= bot.vote %>">
                    Vote</a>
                </li>
            </ul>
            <form class="d-flex">
                <a href="<%= bot.dashboard %>" class="btn btn-outline-success" role="button"><b>Login</b></a>
            </form>
        </div>
    </div>
</nav>
